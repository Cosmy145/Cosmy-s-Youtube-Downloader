[phases.setup]
nixPkgs = ["nodejs_20", "python3", "ffmpeg"]

[phases.install]
cmds = [
  "npm ci",
  "node scripts/setup-yt-dlp.js"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
